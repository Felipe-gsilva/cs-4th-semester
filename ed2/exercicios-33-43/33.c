#include <stdio.h>
#include "33.h"

/*
Implementação de uma Tabela Hash Externa para Armazenar Registros de Alunos

Cada registro deve conter:

    Número de Matrícula: Inteiro
    Nome do Aluno: String

Função de Hash

A função de hash calcula o índice da tabela como o número de matrícula modulado pelo tamanho da tabela:

ındice=matrıcula%tamanho_da_tabela 

Tratamento de Colisões

Se ocorrer uma colisão (ou seja, dois alunos com o mesmo índice), os registros devem ser armazenados em uma lista encadeada (ou em um arquivo separado).
Operações a Implementar

    Inserção: Insira um novo registro de aluno na tabela hash.
    Busca: Dado o número de matrícula, retorne o nome do aluno correspondente.
    Remoção: Remova um aluno da tabela hash dado o número de matrícula.
*/


// must have
// insert
// search
// remove
// h(k)

int h(int k) {
  return 0;
}

int main() {
  node node;
  int i = node.num % 2;

  return 0;
}
